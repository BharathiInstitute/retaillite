# ─── Android Release Signing Configuration ───
# This file is .gitignored — NEVER commit to source control.
#
# Steps to create your release keystore:
#   keytool -genkey -v -keystore retaillite-release.jks \
#     -keyalg RSA -keysize 2048 -validity 10000 -alias retaillite
#
# Then fill in the values below:

storePassword=YOUR_STORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
keyAlias=retaillite
storeFile=../retaillite-release.jks
